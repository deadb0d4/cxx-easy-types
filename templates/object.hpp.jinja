// autogenerated
#pragma once

{% for e in cpp.includes(t) -%}
#include <{{ e }}>
{% endfor %}

namespace {{ cpp.namespace(t) }} {

struct {{t.name}} {
{% for k, v in t.fields.items() -%}
    {{ cpp.typename(v) }} {{ k }};
{% endfor -%}
};

}
